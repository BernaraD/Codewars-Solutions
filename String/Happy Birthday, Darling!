//https://www.codewars.com/kata/5e96332d18ac870032eb735f/solutions/javascript

function womensAge(n) {
  
 let half = Math.floor(n / 2)
 return n % 2 === 0 ? `${n}? That's just 20, in base ${half}!` : 
  `${n}? That's just 21, in base ${half}!`
  
}
