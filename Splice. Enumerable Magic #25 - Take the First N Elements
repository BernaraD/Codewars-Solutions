//https://www.codewars.com/kata/545afd0761aa4c3055001386/train/javascript
function take(arr, n) {
  return arr.length > 0 ? arr.splice(0, n) : []
}
-------------------------
//second solution
function take(arr, n) {
  return arr.slice(0, n);
}
